# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets — never commit
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local smoke test artifacts
.playwright-mcp/
/*.png
/scripts/import-*.mjs
# exceção: o importador do Feedz é o registro de como o histórico entrou —
# o backup é a única cópia desses dados e o Feedz não existe mais.
!/scripts/import-feedz.mjs
/.local/
.vercel

# Supabase CLI local state
supabase/.temp/

# gerado por `deno check` nas edge functions; o deploy do Supabase não o usa
deno.lock
